import { NextResponse } from 'next/server';
import { handleException } from '@/server/utils';
import { {{camelCase nome}}Repository } from '@/server/repository';
import { {{camelCase nome}}Schema } from '@/server/schemas';

async function findAll(req: Request) {
  try {
    const { searchParams } = new URL(req.url);
    const query = {
      page: searchParams.get('page'),
      limit: searchParams.get('limit'),
    };
    const page = Number(query.page) || 1;
    const limit = Number(query.limit) || 10;

    const output = await {{camelCase nome}}Repository.findAll(page, limit);

    return NextResponse.json(output);
  } catch (error) {
    return handleException(error);
  }
}

async function findById(id: string) {
  try {
    const output = await {{camelCase nome}}Repository.findById(id);

    return NextResponse.json(output);
  } catch (error) {
    return handleException(error);
  }
}

async function create(req: Request) {
  try {
    const bodyParsed = await {{camelCase nome}}Schema.create.parse(await req.json());
    await {{camelCase nome}}Repository.create(bodyParsed);

    return NextResponse.json({});
  } catch (error) {
    return handleException(error);
  }
}

async function update(req: Request, id: string) {
  try {
    const bodyParsed = await {{camelCase nome}}Schema.update.parse(await req.json());
    await {{camelCase nome}}Repository.update(id, bodyParsed);

    return NextResponse.json({});
  } catch (error) {
    return handleException(error);
  }
}

async function remove(id: string) {
  try {
    await {{camelCase nome}}Repository.remove(id);

    return NextResponse.json({});
  } catch (error) {
    return handleException(error);
  }
}

export const {{camelCase nome}}Controller = {
  findAll,
  findById,
  create,
  update,
  remove,
};
